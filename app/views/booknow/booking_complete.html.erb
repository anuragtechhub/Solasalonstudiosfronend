<%= react_component 'BookingComplete', {
	professional: JSON.parse(params[:professional]),
	services: JSON.parse(params[:services]),
	time: @time_json,
} %>

<% if Rails.env.test? %>
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" />
<% end %>

<% @body_class = 'sola-search' %>
<% content_for(:title) { t('sola_search.booking_complete') } %>