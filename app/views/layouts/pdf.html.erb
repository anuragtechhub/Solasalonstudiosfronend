<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Sola Salon Studios</title>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  <style type="text/css">
    @font-face {
      font-family: 'CabinRegular';
      font-style: normal;
      font-weight: 300;
      src: url('<%= Rails.root.join('app', 'assets', 'fonts', 'Cabin-Regular.ttf') %>') format('truetype');
    }
    @font-face {
      font-family: 'CabinBold';
      font-style: normal;
      font-weight: 700;
      src: url('<%= Rails.root.join('app', 'assets', 'fonts', 'Cabin-Bold.ttf') %>') format('truetype');
    }
    @font-face {
      font-family: 'LatoLight';
      font-style: normal;
      font-weight: 300;
      src: url('<%= Rails.root.join('app', 'assets', 'fonts', 'Lato-Light.ttf') %>') format('truetype');
    }
  </style>
  <style type="text/css">
    /* global */
    *, *:after, *:before {
      -webkit-box-sizing: border-box;
      -moz-box-sizing: border-box;
      box-sizing: border-box;
    }
    html, body {
      font-family: 'CabinRegular';
      text-rendering: optimize-speed;
    }
    strong {
      font-family: 'CabinBold';
    }

    /* grid */
    .grid:after {
      content: "";
      display: table;
      clear: both;
    }
    [class*='col-'] {
      float: left;
    }
    [class*='col-'] {
      padding-right: 20px;
    }
    [class*='col-']:last-of-type {
      padding-right: 0;
    }    
    .col-2-3 {
      width: 66.66%;
    }
    .col-1-3 {
      width: 33.33%;
    }
    .col-1-2 {
      width: 50%;
    }
    .col-1-4 {
      width: 25%;
    }
    .col-1-8 {
      width: 12.5%;
    }

    /* page styles */
    .page-title {
      font-size: 24px;  
      color: #499CD3;
      margin-bottom: 30px;
      text-align: center;
    }  
    .section {
      background: #499CD3;
      color: #FFFFFF;
      padding: 15px;
      text-align: center;
      text-transform: uppercase;
    }  
    .section-heading {
      font-family: 'LatoLight';
      font-size: 24px;
      margin-bottom: 6px;
    }
    .section-subhead {
      font-family: 'LatoLight';
      font-size: 12px;
    }

    /* tables */
    table.tabular {
      color: #545454;
      width: 100%;
      padding: 20px;
    }
    table.tabular td {
      padding: 15px;
      text-align: center;
    }
    table.tabular td.value {
      font-size: 19px;
      border-right: 1px solid #EFEFEF;
      border-bottom: 2px solid #EFEFEF;
    }
    table.tabular td.label {
      font-size: 11px;
      border-left: 1px solid #EFEFEF;
      border-bottom: 2px solid #EFEFEF;
    }
    table.tabular tr:last-child td.value, table.tabular tr:last-child td.label {
      border-bottom: 0;
    }
  </style>  
</head>
<body>

  <%= yield %>

</body>
</html>