<div class="container">
  <img src="<%= @my_sola_image.generated_image.url(:original) if @my_sola_image && @my_sola_image.generated_image.present? %>" style="width: 100%; height: auto; margin: 30px" />
</div>

<% content_for(:title) { @my_sola_image && @my_sola_image.statement_text.present? ? @my_sola_image.statement_text : "#MySola" } %>
<% content_for(:og_image) { @my_sola_image.generated_image.url(:original) if @my_sola_image && @my_sola_image.generated_image.present? } %>