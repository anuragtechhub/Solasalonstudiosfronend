<div class="container">
	<%= react_component 'SearchBar', {
		date: DateTime.now,
		path: stylist_search_results_path,	
	} %>
</div>

<% @body_class = 'sola-search' %>
<% content_for(:title) { t('page.search.search') } %>