var downloadPDF = function () {
  var req = new XMLHttpRequest();

  req.open("GET", "/pdfs/Sola_Franchise_Guide.pdf", true);

  req.responseType = "blob";

  req.onload = function () {
    var blob = req.response;
    var link = document.createElement('a');
    link.href = window.URL.createObjectURL(blob);
    link.download = "Sola Franchise Guide.pdf";

    link.click();
  };

  req.send();
}

downloadPDF();

jQuery('form').remove();
<% if @success %>
  jQuery('.form_submitted.success').removeClass('hidded');
<% else %>
  jQuery('.form_submitted.error').removeClass('hidded');
<% end %>
jQuery('body').scrollTo('.form_submitted');
