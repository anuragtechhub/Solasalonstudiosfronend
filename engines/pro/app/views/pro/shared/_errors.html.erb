<% if @errors && @errors.length > 0 %>
<ul class="errors" style="<%= defined?(noliststyle) ? 'list-style: none;' : '' %>">
  <% @errors.each do |error| %>
    <li><%= error %></li>
  <% end %>
</ul>
<% end %>
